server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Handle Vue Router history mode (fixes 404 on refresh)
    location / {
        try_files $uri $uri/ /index.html;
    }
}